#!/usr/bin/env python3
#inport the module as a whole
import math
#use the func from the whole module
print (math.factorial(5))


#or import the specific func out of a module
from math import sqrt
#use the func from specific import
print (sqrt(100))
